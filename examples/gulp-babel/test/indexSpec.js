/*global describe: true, it: true, expect: true*/

/**
 * Simple test to check if all works
 */
describe('Index text - ', function () {
    'use strict';

    it('Only to check if all works', function () {
        expect(false).toBeFalsy();
    });
});
